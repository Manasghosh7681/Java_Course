// What is class ?
/* A class is a blueprint or template that defines an object properties and behavior */

//What is Object ?
/* An object is an instance of a class that contains actual value for the properties and can perform the behavior defined by the class */

//What is public static void main(String args[])
/* main method is public because jvm needs to access and call it from outside the class */
/* main method is static because when main method run there is no object created. So how we can call a non-static method that's why main method is static so that jvm call the main method without an object */
/* main method is void because jvm not expect that main method should return any value /*
/* String args[] it is String because Command line argument are recive as a string and args[] because we can take multiple input at a time through command line */

// How java program execute ?
/* java code 1st compile through javac compiler and it convert into class file and that class file is executed by the JVM */

//What is variable ?
/* A variable is a memory location where a value is store and this value can be change */
/* There are 3 types of variable
1. Local variable :Variable which are declare inside the method is called local variable. it is access only inside the method where it is declare.
2. Instance variable :Variable which are declare inside the class but outside the method is call Instance variable .It can be access anywhere inside the class .
3. Static variable :Variable that are declare inside the class but outside method and it mark with static keyword. THis variable are belongs to the class. These are created only once and all the object are share it. It can be access anywhere inside the class .*/